import "./index.css"

import React from 'react'

const Button = ({colorfunction}) => {
  return (


    <button onClick={colorfunction}><strong>New Color</strong></button>
  
  
    )
}

export default Button